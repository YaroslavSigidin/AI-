# Локальные переменные окружения для тестирования
# Этот файл используется только для локального запуска

# Пути к базам данных (относительно корня проекта)
TRACKER_DB_PATH=./local_data/tracker.db
WORKOUT_STATE_DB=./local_data/workout_state.db
USER_SETTINGS_DB=./local_data/user_settings.db

# Отключаем Telegram бота (не используется в локальном режиме)
BOT_TOKEN=

# Отключаем оплаты (не используются в локальном режиме)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# Отключаем внешние сервисы
YC_IAM_TOKEN=
YC_API_KEY=
YC_STT_LANG=ru-RU

# OpenAI API ключ (если нужен для генерации планов)
# OPENAI_API_KEY=your_key_here

# Настройки для локальной разработки
DEBUG=True
LOG_LEVEL=INFO
