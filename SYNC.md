# 🔄 Автоматическая синхронизация с сервером TimeWeb

Эта инструкция поможет настроить автоматическую синхронизацию файлов с сервером при сохранении в Cursor.

## Быстрая настройка

### 1. Настройте конфигурацию (если еще не сделали)

```bash
cp deploy.conf.example deploy.conf
# Отредактируйте deploy.conf и укажите ваши данные сервера
```

### 2. Установите инструмент для мониторинга (выберите один вариант)

#### Вариант 1: fswatch (рекомендуется для macOS)

```bash
brew install fswatch
```

#### Вариант 2: Python watchdog

```bash
pip install watchdog
```

### 3. Запустите автоматическую синхронизацию

Просто запустите:

```bash
./start_sync.sh
```

Или выберите конкретный вариант:

```bash
# Вариант с fswatch (быстрее)
./sync_watch.sh

# Вариант с Python (альтернатива)
python3 sync_watch.py
```

## Как это работает

1. **Первоначальная синхронизация**: При запуске все файлы синхронизируются с сервером
2. **Мониторинг изменений**: Скрипт отслеживает изменения файлов в реальном времени
3. **Автоматическая синхронизация**: При сохранении файла он автоматически отправляется на сервер
4. **Исключения**: Служебные файлы (.bak, .db, .log и т.д.) не синхронизируются

## Использование в Cursor

### Вариант 1: Запуск в отдельном терминале

1. Откройте терминал в Cursor (View → Terminal)
2. Запустите `./start_sync.sh`
3. Оставьте терминал открытым - синхронизация будет работать в фоне
4. Теперь при сохранении файлов они автоматически отправятся на сервер

### Вариант 2: Запуск в фоне

```bash
nohup ./start_sync.sh > sync.log 2>&1 &
```

Чтобы остановить:
```bash
pkill -f sync_watch
```

### Вариант 3: Использование screen/tmux

```bash
# Создайте сессию screen
screen -S sync

# Запустите синхронизацию
./start_sync.sh

# Отключитесь: Ctrl+A, затем D
# Вернуться: screen -r sync
```

## Что синхронизируется

✅ **Синхронизируются:**
- Все `.py` файлы (bot.py, tracker_agent.py и т.д.)
- requirements.txt
- Dockerfile
- Другие конфигурационные файлы

❌ **НЕ синхронизируются:**
- `.bak.*` - резервные копии
- `.db`, `.sqlite` - базы данных
- `.log`, `.pid` - логи и PID файлы
- `.env` - секретные данные
- `.git/` - git репозиторий
- `__pycache__/`, `.pyc` - кэш Python
- `venv/`, `.venv/` - виртуальные окружения
- `deploy.conf` - локальная конфигурация

## Перезапуск бота после изменений

⚠️ **Важно:** Автоматическая синхронизация **не перезапускает** бота автоматически!

После внесения изменений в код нужно перезапустить бота на сервере вручную:

```bash
ssh user@server
cd /path/to/project
./restart.sh
```

Или настройте автоматический перезапуск через команду в `deploy.conf`:

```bash
# В deploy.conf укажите:
RESTART_COMMAND="cd $SERVER_PATH && ./restart.sh"
```

Тогда можно использовать обычный деплой для перезапуска:

```bash
./deploy.sh  # Сделает полную синхронизацию + перезапуск
```

## Решение проблем

### Ошибка "fswatch не найден"

Установите через Homebrew:
```bash
brew install fswatch
```

### Ошибка "watchdog не найден"

Установите через pip:
```bash
pip install watchdog
```

### Ошибка подключения по SSH

1. Проверьте настройки в `deploy.conf`
2. Убедитесь, что SSH ключи настроены:
   ```bash
   ssh-copy-id user@server
   ```
3. Проверьте подключение:
   ```bash
   ssh user@server
   ```

### Файлы не синхронизируются

1. Проверьте права на файлы:
   ```bash
   chmod +x sync_watch.sh start_sync.sh
   ```

2. Проверьте логи синхронизации:
   ```bash
   # Если запускали в фоне
   tail -f sync.log
   ```

3. Убедитесь, что файл не в списке исключений

### Синхронизация слишком медленная

- Используйте `fswatch` вместо Python watchdog (быстрее)
- Уменьшите количество синхронизируемых файлов
- Проверьте скорость интернет-соединения

## Рекомендации

1. **Используйте fswatch на macOS** - он быстрее и эффективнее
2. **Запускайте в screen/tmux** - чтобы не терять процесс при закрытии терминала
3. **Не редактируйте файлы напрямую на сервере** - они будут перезаписаны при следующей синхронизации
4. **Используйте Git** - для контроля версий и резервного копирования
5. **Делайте полный деплой перед важными изменениями** - для перезапуска бота

## Безопасность

⚠️ Файл `deploy.conf` содержит чувствительные данные и **не должен** попадать в Git (уже добавлен в `.gitignore`).
