# Минимальные зависимости для локального прототипа
# Без Telegram бота, оплат и внешних сервисов

# FastAPI и сервер
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Для работы с базами данных (уже в стандартной библиотеке, но для совместимости)
# sqlite3 встроен в Python

# Для работы с датами и временем (уже в стандартной библиотеке)
# datetime встроен в Python

# Для JSON (уже в стандартной библиотеке)
# json встроен в Python

# Опционально: для работы с OpenAI API (если нужна генерация планов)
# openai>=1.0.0

# Для работы с профилями и статистикой (если используются)
# numpy>=1.26.3  # для stats_enhanced
# matplotlib>=3.8.2  # для графиков статистики

# Примечание: 
# - aiogram не нужен (Telegram бот не запускается)
# - aiohttp не нужен (если не используются внешние API)
# - Все остальные зависимости опциональны
