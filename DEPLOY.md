# 🚀 Инструкция по автоматическому развертыванию

## Быстрый старт

### 1. Настройка локально

1. Скопируйте файл конфигурации:
   ```bash
   cp deploy.conf.example deploy.conf
   ```

2. Отредактируйте `deploy.conf` и укажите ваши данные:
   - `85.193.89.214` - адрес вашего сервера TimeWeb
   - `root` - имя пользователя SSH
   - `/root/ai_trainer_cursor` - путь к проекту на сервере
   - `-p 22` - команда для перезапуска бота

3. Настройте SSH ключи (рекомендуется):
   ```bash
   ssh-copy-id user@your-server.timeweb.ru
   ```

### 2. Ручной деплой

Запустите скрипт деплоя:
```bash
chmod +x deploy.sh
./deploy.sh
```

### 3. Настройка на сервере

Подключитесь к серверу и создайте скрипт перезапуска:

```bash
# На сервере
cd /path/to/your/project
cp restart.sh.example restart.sh
chmod +x restart.sh
```

## Автоматический деплой через GitHub Actions

Если ваш проект в Git, можно настроить автоматический деплой при каждом push.

### Настройка секретов в GitHub

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте следующие секреты:

   - `SSH_PRIVATE_KEY` - приватный SSH ключ для подключения к серверу
   - `SERVER_HOST` - адрес сервера (например: `server.timeweb.ru`)
   - `SERVER_USER` - имя пользователя SSH
   - `SERVER_PATH` - путь к проекту на сервере (например: `/home/user/ai_trainer_cursor`)
   - `SSH_OPTS` - дополнительные опции SSH (можно оставить пустым или указать порт: `-p 2222`)

### Генерация SSH ключа (если еще нет)

```bash
ssh-keygen -t rsa -b 4096 -C "github-actions"
# Сохраните в ~/.ssh/github_deploy
```

Публичный ключ добавьте на сервер:
```bash
cat ~/.ssh/github_deploy.pub | ssh user@server 'cat >> ~/.ssh/authorized_keys'
```

Приватный ключ добавьте в GitHub Secrets как `SSH_PRIVATE_KEY`:
```bash
cat ~/.ssh/github_deploy
# Скопируйте весь вывод и вставьте в GitHub Secrets
```

## Варианты перезапуска бота

В зависимости от того, как запущен ваш бот, используйте соответствующую команду в `deploy.conf`:

### Вариант 1: systemd
```bash
RESTART_COMMAND="sudo systemctl restart ai_trainer_bot"
```

Создайте сервис `/etc/systemd/system/ai_trainer_bot.service`:
```ini
[Unit]
Description=AI Trainer Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/ai_trainer_cursor
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Вариант 2: supervisor
```bash
RESTART_COMMAND="sudo supervisorctl restart ai_trainer_bot"
```

### Вариант 3: pm2
```bash
RESTART_COMMAND="pm2 restart ai_trainer_bot"
```

### Вариант 4: Простой скрипт (restart.sh)
```bash
RESTART_COMMAND="cd $SERVER_PATH && ./restart.sh"
```

Это самый простой вариант - используйте скрипт `restart.sh`, который будет на сервере.

## Проверка работы

После деплоя проверьте, что бот работает:

```bash
ssh user@server
cd /path/to/project
tail -f bot.log
# или
ps aux | grep bot.py
```

## Решение проблем

### Ошибка подключения по SSH
- Проверьте, что SSH ключ добавлен на сервер
- Убедитесь, что `SERVER_HOST` и `SERVER_USER` указаны правильно
- Проверьте доступность порта SSH (обычно 22)

### Бот не перезапускается
- Проверьте права на выполнение `restart.sh`: `chmod +x restart.sh`
- Проверьте, что команда в `RESTART_COMMAND` корректна
- Попробуйте выполнить команду вручную на сервере

### Ошибки при установке зависимостей
- Убедитесь, что на сервере установлен Python 3.11+
- Проверьте, что pip установлен и обновлен
- Если используете venv, убедитесь что он активирован в `restart.sh`

## Безопасность

⚠️ **Важно:**
- Файл `deploy.conf` содержит чувствительные данные - не коммитьте его в Git!
- Он уже добавлен в `.gitignore`
- SSH ключи храните в безопасном месте
- Используйте ключи с паролями для дополнительной защиты
# deploy test
